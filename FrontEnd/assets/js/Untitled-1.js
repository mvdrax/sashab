//Methode / Requete C.R.U.D :

// Create (envoyer):

// method: "post",

// header si dessous pour la requete login (page de connection et recupération du token):
// headers: { "Content-Type": "application/json" },
// body: JSON.stringify({
//      email: constEmail,
//      password: constPassword
// });

// header si dessous pour la requete post img/work (pour ajouter une image):
// headers: { "Authorization": `Bearer ${NomDeVariableQuiContientLeToken}` },

